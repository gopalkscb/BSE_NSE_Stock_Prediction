import { InternalExpandableRowsProps } from '../expandable-rows/expandable-rows-utils';
import { TableProps } from '../interfaces';
import { InternalSelectionType } from '../internal-interfaces';
import { SelectionProps } from './interfaces';
type SelectionOptions<T> = Pick<TableProps<T>, 'ariaLabels' | 'items' | 'trackBy' | 'getLoadingStatus' | 'selectedItems' | 'totalItemsCount'> & {
  rootItems: readonly T[];
  selectionType?: InternalSelectionType;
  expandableRows: InternalExpandableRowsProps<T>;
  setLastUserAction?: (name: string) => void;
};
export declare function useGroupSelection<T>({
  ariaLabels,
  items,
  rootItems,
  selectedItems,
  totalItemsCount,
  trackBy,
  expandableRows,
  selectionType,
  getLoadingStatus,
  setLastUserAction
}: SelectionOptions<T>): SelectionProps<T>;
export {};